<h1>New Wishlist</h1>




<div class="container">
  <div class="jumbotron">
    <%=form_for @wishlist, method: 'POST' do |xx| %>
      <p> <%= xx.hidden_field :user_id, :value => @uuid%></p>
      <p> <%= xx.hidden_field :project_id, :value => @ppid%></p>
      <a class="btn btn-primary m-1"  role="button"><%= xx.submit  %></a>
      </div>
    <%end%>



<%= link_to 'Back', wishlists_path %>



<%= @uuid %>
<%= @ppid%>
